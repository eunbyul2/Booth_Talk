# 전시회 기반 페이지 연동 완료

## ✅ 작업 완료 사항

### 1. VisitorHome.jsx 수정
**위치**: `d:\Booth_Talk\frontend\src\pages\visitor\VisitorHome.jsx`

**변경 내용**:
- ❌ 기존: 개별 이벤트 목록 표시
- ✅ 변경: 전시회(Exhibition) 목록 표시
- 전시회 클릭 시 → `/visitor/events?exhibition_id={id}` 로 이동

**주요 기능**:
```javascript
// 전시회 데이터 구조
{
  id: 1,
  name: '2025 코엑스 푸드위크',
  code: 'S0902',
  startDate: '2025-10-29',
  endDate: '2025-11-01',
  hallInfo: '제1전시관 A, B, C',
  venueName: '코엑스',
  eventCount: 5,  // 참여 업체 수
  ...
}
```

### 2. EventList.jsx 수정
**위치**: `d:\Booth_Talk\frontend\src\pages\visitor\EventList.jsx`

**변경 내용**:
- URL 파라미터로 `exhibition_id` 받기
- 해당 전시회의 이벤트만 필터링하여 표시
- 전시회 정보를 상단에 표시

**주요 기능**:
```javascript
// URL에서 exhibition_id 추출
const [searchParams] = useSearchParams()
const exhibitionId = searchParams.get('exhibition_id')

// exhibition_id에 따라 데이터 로드
useEffect(() => {
  if (exhibitionId) {
    // 해당 전시회 데이터 로드
    setExhibition(...)
    setEvents(...)
  }
}, [exhibitionId])
```

## 📱 사용자 플로우

```
1. /visitor 페이지 접속
   ↓
2. 전시회 목록 확인
   - 2025 코엑스 푸드위크
   - 2025 킨텍스 테크 엑스포
   - 2025 부산 모빌리티 쇼
   ↓
3. 전시회 선택 (예: 코엑스 푸드위크)
   ↓
4. /visitor/events?exhibition_id=1 로 이동
   ↓
5. 해당 전시회의 참여 업체 이벤트 5개 표시
   - [S0902] 농업회사법인(주)해남진호드모
   - [B5201] (주)대일피비
   - [특별관] 헬스클럽레저 컴퍼니
   - [B5001] 협찬투어
   - [S0902] 농업회사법인(주)해남진호드모
```

## 🎯 화면 구성

### /visitor 페이지
```
┌─────────────────────────────────────┐
│  Hero Section                       │
│  - 검색 바                          │
│  - 통계 (3개 전시회, 19개 기업)    │
├─────────────────────────────────────┤
│  지도                               │
│  - 전시회 마커 표시 (빨간색 테두리) │
│  - 클릭 시 InfoWindow + 이동 버튼   │
├─────────────────────────────────────┤
│  진행 예정 전시회 리스트            │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 🖼️ │ S0902                   │ │
│  │     │ 2025 코엑스 푸드위크    │▶│
│  │     │ 📍 코엑스 제1전시관... │ │
│  │     │ 📅 10.29(수) ~ 11.1(토)│ │
│  │     │ 🏢 참여 업체 5개        │ │
│  └───────────────────────────────┘ │
│                                     │
│  (전시회 3개 반복)                  │
└─────────────────────────────────────┘
```

### /visitor/events?exhibition_id=1 페이지
```
┌─────────────────────────────────────┐
│  검색 바                            │
├─────────────────────────────────────┤
│  10.29(수) 11:00                    │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ S0902                         │ │
│  │ 2025 코엑스 푸드위크          │ │
│  │ 📆 10.29(수) ~ 11.1(토)       │ │
│  │ 📍 제1전시관 A, B, C          │ │
│  └───────────────────────────────┘ │
│                                     │
│  참여 업체 이벤트 (5개)             │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ 🖼️ │ B5001                   │ │
│  │     │ [S0902] 농업회사법인... │▶│
│  │     │ 🕐 11:00 13:00 14:00... │ │
│  │     │ 당일 조달로 시작        │ │
│  │     │ 🎁 무료 시식, 할인...  │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

## 🔗 데이터 연관성

### 현재 구조 (MOCK 데이터)
```
VisitorHome.jsx
  └── MOCK_EXHIBITIONS (3개)
        └── id: 1, 2, 3

EventList.jsx
  └── exhibition_id 파라미터로 필터링
        └── MOCK_EXHIBITION (id: 1)
              └── MOCK_EVENTS (5개)
```

### 향후 API 연동 시
```
GET /api/visitor/exhibitions
  → 전시회 목록 반환

GET /api/visitor/events?exhibition_id=1
  → 특정 전시회의 이벤트 목록 반환
  → { exhibition: {...}, events: [...] }
```

## 📊 샘플 데이터

### 전시회 3개
1. **2025 코엑스 푸드위크** (S0902)
   - 기간: 10.29 ~ 11.1
   - 장소: 코엑스 제1전시관 A, B, C
   - 참여 업체: 5개

2. **2025 킨텍스 테크 엑스포** (T1105)
   - 기간: 11.5 ~ 11.8
   - 장소: 킨텍스 제2전시관 전관
   - 참여 업체: 8개

3. **2025 부산 모빌리티 쇼** (M1120)
   - 기간: 11.20 ~ 11.23
   - 장소: 벡스코 제1전시장
   - 참여 업체: 6개

### 이벤트 5개 (코엑스 푸드위크)
1. 농업회사법인 (B5001)
2. (주)대일피비 (B5201)
3. 특별한헬스클럽 (A-312)
4. 협찬투어 (B5001)
5. 농업회사법인 (S0902)

## 🎨 스타일 특징

### VisitorHome
- 전시회 마커: 빨간색 테두리 (#FF6B6B)
- 호버 시: 마커 크기 증가 (12px → 16px)
- 카드 호버: 빨간색 테두리 + 그림자

### EventList
- 행사 카드: 빨간색 그라데이션 배경
- 시간대 배지: 파란색
- 혜택 정보: 초록색

## 🔧 테스트 방법

### 1. 개발 서버 실행
```bash
cd frontend
npm start
```

### 2. 테스트 시나리오

#### 시나리오 1: 전시회 선택
1. `http://localhost:3000/visitor` 접속
2. "2025 코엑스 푸드위크" 카드 클릭
3. URL이 `/visitor/events?exhibition_id=1` 로 변경되는지 확인
4. 상단에 전시회 정보 표시 확인
5. 하단에 5개 이벤트 표시 확인

#### 시나리오 2: 지도 마커 클릭
1. `http://localhost:3000/visitor` 접속
2. 지도의 빨간색 마커 클릭
3. InfoWindow 표시 확인
4. "참여 업체 보기" 버튼 클릭
5. `/visitor/events?exhibition_id=1` 로 이동 확인

#### 시나리오 3: 직접 URL 접속
1. `http://localhost:3000/visitor/events?exhibition_id=1` 직접 접속
2. 전시회 정보와 이벤트 목록 정상 표시 확인

#### 시나리오 4: 뒤로 가기
1. `/visitor/events?exhibition_id=1` 에서
2. "← 홈으로" 버튼 클릭
3. `/visitor` 로 이동 확인

## 💡 주요 변경점

### Before (기존)
```
/visitor
  └── 개별 이벤트 목록 (5개)
        └── 클릭 → /visitor/event/{id}

/visitor/events
  └── 모든 이벤트 목록
        └── 독립적으로 동작
```

### After (변경 후)
```
/visitor
  └── 전시회 목록 (3개)
        └── 클릭 → /visitor/events?exhibition_id={id}

/visitor/events?exhibition_id=1
  └── 특정 전시회 정보 + 해당 전시회의 이벤트 목록
        └── 연관성 있는 데이터 표시
```

## 🚀 향후 작업

### Backend API 연동
```javascript
// VisitorHome.jsx
const fetchExhibitions = async () => {
  const response = await fetch('/api/visitor/exhibitions')
  const data = await response.json()
  setExhibitions(data.exhibitions)
}

// EventList.jsx
const fetchExhibitionEvents = async (exhibitionId) => {
  const response = await fetch(`/api/visitor/events?exhibition_id=${exhibitionId}`)
  const data = await response.json()
  setExhibition(data.exhibition)
  setEvents(data.events)
}
```

### Database 쿼리
```sql
-- 전시회 목록
SELECT * FROM exhibitions WHERE is_active = TRUE;

-- 특정 전시회의 이벤트
SELECT 
  e.*,
  c.company_name,
  ex.exhibition_name
FROM events e
JOIN companies c ON e.company_id = c.id
JOIN exhibitions ex ON e.exhibition_id = ex.id
WHERE e.exhibition_id = 1;
```

## ✨ 완료!

모든 페이지가 전시회 기반으로 연동되었습니다:
- ✅ `/visitor` - 전시회 목록 표시
- ✅ `/visitor/events?exhibition_id={id}` - 특정 전시회의 이벤트 표시
- ✅ 데이터 연관성 구현
- ✅ URL 파라미터 전달
- ✅ 샘플 데이터 3개 전시회, 각각 이벤트 포함

현재는 MOCK 데이터를 사용하고 있으며, DB 연결 시 API만 연동하면 바로 사용 가능합니다! 🎉
