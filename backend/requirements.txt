# FastAPI 프레임워크
fastapi==0.104.1
uvicorn[standard]==0.24.0

# 데이터베이스
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1

# 인증 및 보안
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
bcrypt==4.1.1

# 환경 변수
python-dotenv==1.0.0

# 기타 유틸리티
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0
dnspython==2.4.2
qrcode==7.4.2

# OCR (선택사항 - 필요시 주석 해제)
# pytesseract==0.3.10
# Pillow==10.1.0

# 파일 업로드
aiofiles==23.2.1

# CORS는 FastAPI에 내장되어 있음
# from fastapi.middleware.cors import CORSMiddleware 사용

# ERD 자동 생성 (선택사항 - 개발용)
# eralchemy2==1.3.8
# 설치: pip install eralchemy2
